package com.seoulink.backend.domain.course.service;

import org.springframework.stereotype.Service;

/**
 * 최적화가 끝난 코스와 장소별 방문 순서를 DB에 저장하는 서비스이다.
 *
 * <p>현재는 서비스 역할만 정의한다. {@code TravelCourse}, {@code CourseDetail}의
 * 엔티티 매핑과 DB 통합이 끝난 뒤 Repository를 연결해 저장 로직을 구현한다.</p>
 */
@Service
public class CourseSaveService {
    // TODO: DB 통합 후 TravelCourse와 CourseDetail 저장 로직을 구현한다.
}
