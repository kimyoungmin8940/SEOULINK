/**
 * 여행 유형 정보를 외부 API로 전달할 때 사용할 DTO 패키지이다.
 *
 * <p>현재 여행 유형은 설문 계산과 장소 연결에 사용하는 내부 기준 데이터이고,
 * 별도의 컨트롤러·서비스·API 명세가 아직 없으므로 DTO 클래스가 비어 있어도 정상이다.
 * 추후 여행 유형 조회 API가 필요해지면 {@code TravelTypeResponse} 같은 DTO를 추가한다.</p>
 */
package com.seoulink.backend.domain.traveltype.dto;
