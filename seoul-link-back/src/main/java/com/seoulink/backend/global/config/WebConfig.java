package com.seoulink.backend.global.config;

/**
 * SEOULINK의 웹 계층 공통 설정을 담당할 클래스이다.
 *
 * <p>프론트엔드와의 CORS 설정, 인터셉터, 정적 리소스 경로,
 * 날짜 형식 변환처럼 여러 도메인에서 공통으로 사용하는 MVC 설정을 이곳에 둔다.</p>
 *
 * <p>현재는 패키지 구조를 먼저 잡아 둔 상태이며, 실제 설정이 필요할 때
 * {@code WebMvcConfigurer} 구현 및 {@code @Configuration} 등록을 추가한다.</p>
 */
public class WebConfig {
    // TODO: 담당 기능의 요구사항과 API 명세가 확정되면 구현한다.
}
