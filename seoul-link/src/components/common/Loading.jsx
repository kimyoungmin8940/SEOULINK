/**
 * TODO: API 응답을 기다리는 동안 보여줄 공통 로딩 컴포넌트입니다.
 *
 * 화면 전체 로딩과 카드 내부 로딩을 구분할 수 있도록 `variant`를 받고,
 * 필요한 경우 안내 문구를 `message`로 전달하는 구조를 권장합니다.
 * 단순 애니메이션만 보여주지 말고 role="status", aria-live="polite"와
 * 화면 낭독용 문구를 함께 제공해야 합니다.
 */
