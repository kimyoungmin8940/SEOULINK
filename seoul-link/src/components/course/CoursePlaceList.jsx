/**
 * TODO: 한 코스에 포함된 장소를 방문 순서대로 보여주는 컴포넌트입니다.
 *
 * `places` 배열을 받아 순번, 장소명, 카테고리, 예상 체류 시간과
 * 다음 장소까지의 이동 거리/시간을 표시합니다. 코스 편집 화면에서 재사용할 경우
 * onMove, onRemove 같은 콜백을 선택적으로 받아 순서 변경과 삭제를 지원합니다.
 * 배열 key에는 순번이 아닌 placeId 또는 coursePlaceId를 사용합니다.
 */
