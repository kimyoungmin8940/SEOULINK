/**
 * TODO: 추천 및 전체 코스 목록의 검색 조건을 선택하는 필터 컴포넌트입니다.
 *
 * 테마, 지역, 소요 시간, 이동 수단 등의 현재 값을 `value`로 받고
 * 사용자가 변경하면 `onChange`로 부모 화면에 전달하는 제어 컴포넌트로 만듭니다.
 * 필터 자체가 API를 직접 호출하지 않고, 부모가 변경된 조건을 query string으로
 * 만들어 `getCourses`를 다시 호출하도록 역할을 분리합니다.
 */
