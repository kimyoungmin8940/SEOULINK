/**
 * TODO: 지도와 장소 목록에서 사용할 장소 검색 필터입니다.
 *
 * 키워드, 카테고리, 자치구, 영업 여부 등의 값을 부모에게 전달하는
 * 제어 컴포넌트로 구현합니다. 필터가 변경될 때마다 즉시 요청할 경우에는
 * 검색어 입력에 debounce를 적용하고, 지도 범위 기반 검색은 현재 bounds와
 * 함께 백엔드 장소 조회 API로 전달합니다.
 */
