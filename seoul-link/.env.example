# Toss Payments payment-widget sandbox client key. Restart Vite after changing this file.
VITE_TOSS_CLIENT_KEY=test_gck_docs_Ovk5rk1EwkEbP0W43n07xlzm


//.env.example은 실제로 환경변수를 읽는 파일이 아니라, 프로젝트를 받은 사람이 어떤 환경변수를 설정해야 하는지 알려주는 예시 파일입니다.
// echo $env:OPENAI_API_KEY
   echo $env:TOSS_SECRET_KEY
   echo $env:VITE_TOSS_CLIENT_KEY
   토스 클라이언트 키와 토스 시크릿 키는 토스 공식 문서에 나와있는 테스트 키
   토스 클라이언트 키는 .env 파일에 쓰고 , open ai api 와 토스 시크릿 키는 환경변수로 설정한다.
